server:
  port: 8090

spring:
  application:
    name: glxt-api
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}

common:
  execute_test_max_count: 100
  sql_result_page_max_row: 5000

  demo_path: D:/demo/interface
